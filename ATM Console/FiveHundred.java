public class FiveHundred extends Notes{

    //constructor with note and count parameters
    public FiveHundred(int note, int count){
        super(note,count);// call the constructor of the parent class (Notes) with the note and count arguments.
    }
}
